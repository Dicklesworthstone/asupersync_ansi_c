[toolchain]
# Keep RCH from wrapping C-only `make` lanes with a host-specific dated nightly
# inferred from the operator machine. Generic nightly is sufficient for the
# auxiliary Rust tooling under `tools/` and is present across the worker fleet.
channel = "nightly-2026-08-31"
components = ["rustfmt", "clippy"]
profile = "default"
